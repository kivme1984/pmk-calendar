name: V51 Chrome UI check v2

on:
  pull_request:
    branches: [main]

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Install driver
        run: |
          npm init -y
          npm install playwright-core@1.53.0
      - name: Start site
        run: |
          mkdir -p artifacts
          python3 -m http.server 4173 > /tmp/pmk-http.log 2>&1 &
          sleep 2
      - name: Check v51 mobile UI
        run: node tests/v51_ui_smoke_v2.mjs
      - name: Upload diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: v51-mobile-ui-v2
          path: artifacts/*
          if-no-files-found: ignore
